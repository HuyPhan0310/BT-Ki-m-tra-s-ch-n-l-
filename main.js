var x;
// Lấy giá trị từ người dùng
x = Number(prompt("Nhập vào số bất kỳ", "0"));
// Kiểm tra số chẵn và lẽ
if (x % 2 == 0) {
    document.write("x là số chẵn");
} else {
    document.write("x là số lẻ");
}
